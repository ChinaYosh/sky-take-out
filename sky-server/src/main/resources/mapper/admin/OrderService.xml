<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.admin.OrderMapper">

    <select id="selectSalesTop" resultType="com.sky.dto.GoodsSalesDTO">
        SELECT od.name,sum(od.number) number
        from sky_take_out.orders o join sky_take_out.order_detail od
        on od.order_id = o.id
        <where>
         o.status = 5

        and o.order_time between #{left} and #{right}

        </where>
        group by od.name
        order by number desc
        limit 10
    </select>
</mapper>
